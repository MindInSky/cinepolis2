/*
movlist.forEach(function(element) {
  let movieName = element.querySelectorAll(".datalayer-movie");
  let movieHours = element.querySelectorAll(".btnhorario");
  movieName.forEach(function(element) {
    console.log(element.innerText());
  });
  movieHours.forEach(function(element) {
    console.log(element.innerText());
  });
});
//edsd
movlist.forEach(function(element) {
  let movieName = element.querySelectorAll(".datalayer-movie");
  let movieHours = element.querySelectorAll(".btnhorario");
  console.log(movieName);
  console.log(movieHours);
});*/
